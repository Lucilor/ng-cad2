import {State} from "./state";

export const getLoading = (state: State) => state.loading;

export const getCurrCads = (state: State) => state.currCads;

export const getCadStatus = (state: State) => state.cadStatus;
